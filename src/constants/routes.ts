import Page from "@/types/Page";
import Route from "@/types/Route";

const routes: Route[] = [
  {
    page: Page.HOME,
    path: "/",
  },
  {
    page: Page.MUSIC,
    path: "/music",
  },
];

export default routes;
